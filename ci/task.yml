---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: kennethreitz/pipenv
inputs:
- name: concourse-demo-python-service
run:
  path: sh
  dir: concourse-demo-python-service
  args:
  - -exc
  - |
    pipenv install
    pipenv run python -m pytest